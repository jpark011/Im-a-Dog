<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    package="com.cs446w18.a16.imadog">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity android:name=".activities.menu.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.menu.MainActivity" />

        <activity android:name=".activities.menu.JoinGameActivity" />

        <activity android:name=".activities.menu.CreateGameActivity" />

        <activity android:name=".activities.menu.SettingsActivity" />

        <activity android:name=".activities.menu.LobbyActivity" />

        <activity  android:name=".activities.HelpActivity" />

        <activity android:name=".activities.GameActivity" />

        <activity android:name=".activities.ProfileActivity" />




    </application>

</manifest>